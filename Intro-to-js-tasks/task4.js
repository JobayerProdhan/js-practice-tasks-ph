var a = isNaN("11");

var a = isNaN(2-10);

/**
 *  isNaN is a JS function that check if a value is Not-a Number.
 *              --> It returns true if the value cannot be converted to a number otherwise it's return false 
 * 
 * Second line of the above code: "11" is successfully return a number . the isNaN function return false 
 */